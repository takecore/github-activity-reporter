# Github Activity Reporter

## Install

```
npm install
```

## Usage

```sh
$ node index.js --help

Github Activity Reporter

  github happy activity reporter 🚀

Synopsis

  $ node index.js <github username> <github personal access token>
  $ node index.js <github username> <github personal access token> -l en --from
  2019-01-01 --until 2019-02-01

Options

  -l, --language string   ja or en are available.
  -f, --from string       get data from this date param, format YYYY-MM-DD.
  -u, --until string      get data until this date param, format YYYY-MM-DD.
  -h, --help
```
